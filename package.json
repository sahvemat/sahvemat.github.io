{
  "name": "sahmat-magazine",
  "private": true,
  "version": "1.0.0",
  "description": "Minimal Node wrapper for AI Studio compatibility. Not used in GitHub Pages deployment.",
  "scripts": {
    "build": "echo 'Jekyll build will be handled by GitHub Pages'",
    "clean": "rm -rf _site",
    "lint": "echo 'No linting needed'"
  }
}
